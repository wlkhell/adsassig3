#include <vector>
#include <string>
using namespace std;
int linearSearch(const vector<string>& dir, const string& target) {
    for (int i = 0; i < dir.size(); i++) {
        if (dir[i] == target) {
            return i;
        }
    }
    return -1;
}
